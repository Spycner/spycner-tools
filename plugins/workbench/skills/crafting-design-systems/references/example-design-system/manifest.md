# Example Design System

A fully-populated example showing the directory shape and the kinds of values to override. Inspired by a "twilight" palette: deep navy paper, ivory ink, warm coral accent. Use this as a reference when creating your own design system; do not copy it as the active system.
